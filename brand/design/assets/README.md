# Logo

Drop the official YD logo here as `yd-logo.svg` (preferred) or `yd-logo.png`
(transparent background). `render.js` picks it up automatically and uses it as the
soft centered watermark instead of the typeset "YD" monogram.
